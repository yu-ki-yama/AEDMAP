
  <% aed_inf_array.each_with_index do |aed_inf, index| %>
    <% if (index+1)%3 == 0 %>
      <div class="list-row"><div><%= aed_inf['created_at'].strftime('%Y/%m/%d') %>　<%= aed_inf['facility'] %></div> <%= link_to "編集", edit_aed_information_management_path(aed_inf['id']),class: "edit-aed-inf btn btn-primary" %></div><br>
    <% else %>
      <div class="list-row"><div><%= aed_inf['created_at'].strftime('%Y/%m/%d') %>　<%= aed_inf['facility'] %></div> <%= link_to "編集", edit_aed_information_management_path(aed_inf['id']),class: "edit-aed-inf btn btn-primary" %></div>
    <% end %>
  <% end %>
